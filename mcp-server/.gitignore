node_modules/
.env

# Google service account keys. These live inside the repo directory for
# convenience, so keep the patterns broad — a leaked key grants write access
# to the whole Groundwork sheet.
thrive-sa.json
*-sa.json
*service-account*.json
*.key.json
